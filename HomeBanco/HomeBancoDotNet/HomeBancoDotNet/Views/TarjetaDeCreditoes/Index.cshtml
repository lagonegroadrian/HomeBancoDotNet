@model IEnumerable<HomeBancoDotNet.Models.TarjetaDeCredito>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.titular)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.numero)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.codigoV)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.limite)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.consumos)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.titular.idUsuario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.numero)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.codigoV)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.limite)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.consumos)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idTarjetaDeCredito">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idTarjetaDeCredito">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idTarjetaDeCredito">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
